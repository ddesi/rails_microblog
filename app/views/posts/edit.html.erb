<div id="profile">
	<h1>Edit <%= @post.user.username %>'s Post:</h1>

	<p><%= @post.body %></p>

	<%= form_for @post do |p| %>
	  
	  <p align="center"><%= p.text_area :body, size: "80x20" %></p>

	  <%= p.submit :update %>

	<% end %>
</div>